; HELLFALL - Config/DefaultEditor.ini (REQ-G1-001).
; Intentionally minimal. Per-user editor preferences do not belong in the repo; the level generators
; (Tools/ue/*.py) run headless through UnrealEditor-Cmd.exe and need nothing from here.

[/Script/UnrealEd.EditorPerProjectUserSettings]
; no project-wide overrides
