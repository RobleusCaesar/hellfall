# BUILD.md - decision log

Required by spec ground rule G-5 and REQ-G1-001 / REQ-G6-007. This file records the exact build identity, the review machine, every decision the specification left to the agent, every Blueprint (none), the asset working set, third-party licences, and a dated change log. It is updated at every gate and never rewritten from scratch.

---

## 1. Build identity

| Component | Version | State on the build machine, 2026-09-06 |
|---|---|---|
| **Unreal Engine** | **5.8.2** - `C:\Program Files\Epic Games\UE_5.8\Engine\Build\Build.version`: MajorVersion 5, MinorVersion 8, PatchVersion 2, Changelist **56702186**, CompatibleChangelist 55116800, branch `++UE5+Release-5.8`. Recorded at install on 2026-09-06 and **never changed mid-project (G-1)**. 5.8 was released 2026-06-17 and is Epic's last planned UE5 major. | **Installed 2026-09-06** through the Epic Games Launcher at `C:\Program Files\Epic Games\UE_5.8`; `UnrealEditor-Cmd.exe`, `RunUAT.bat`, `Build.bat`, `Engine\Plugins\Experimental\PythonScriptPlugin` and the embedded Python 3.11.8 are all present. **Registry caveat:** the Launcher has not (yet) written `HKLM\SOFTWARE\EpicGames\Unreal Engine\5.8` and `LauncherInstalled.dat` still lists no installation, so engine resolution must also try the default folder `C:\Program Files\Epic Games\UE_<EngineAssociation>` and `$env:UE_ROOT` (`Tools/ue/_engine.ps1` tries `UE_ROOT`, then the default Launcher folder - accepted only when its `Engine\Build\Build.version` matches - then HKLM, then HKCU source builds). |
| Blender | 5.2.1 LTS (blender.org) | Installed at `C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`. **Ran the REQ-G2-001 audit on 2026-09-07** (`Tools/blender/audit_glb.py`, headless `--background --factory-startup`, bundled glTF importer 5.2.40: 17 GLBs, 0 errors, 6.3 s; section 5). The Gate 4 export scripts join it under `Tools/blender/`. |
| Visual Studio 2022 Build Tools | **17.14.37614** (vswhere `installationVersion` 17.14.37614.0, display version 17.14.39); `VCTools` workload; **MSVC 14.44.35207** x64 (UnrealBuildTool reports it as the `14.44.35228` toolchain); **Windows SDK 10.0.26100**; .NET Framework 4.6.2 targeting pack | **Installed 2026-09-06** by Rob through the Build Tools installer (the agent's two winget attempts had been cancelled at the UAC prompt). `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`. UBT accepted the 14.44 toolset with no toolset complaint (`Using Visual Studio 14.44.35228 toolchain (...\MSVC\14.44.35207) and Windows 10.0.26100.0 SDK`), which closes the `Docs/TOOLCHAIN-SETUP.md` question about v14.50. |
| .NET Framework SDK | **4.8.1 Developer Pack** - `C:\Program Files (x86)\Windows Kits\NETFXSDK\4.8.1` | **Installed 2026-09-06** via winget (`Microsoft.DotNet.Framework.DeveloperPack_4`, one UAC click by Rob) after the first build failed at rules time: UE's `Engine/Source/Editor/SwarmInterface/SwarmInterface.Build.cs` throws on Win64 without it (`Could not find NetFxSDK install dir; this will prevent SwarmInterface from installing.  Install a version of .NET Framework SDK at 4.6.0 or higher.`). The Build Tools component `Microsoft.Net.Component.4.8.SDK` is the equivalent; `setup.exe modify` to add it returned exit 5007 (installer blocked pending a reboot), hence the standalone pack. Required step in `Docs/TOOLCHAIN-SETUP.md` (step 2b). |
| .NET SDK | 8.0.424 (UnrealBuildTool / UAT host) | Installed at `C:\Program Files\dotnet\sdk`. |
| Python (tooling) | 3.12.10 at `C:\Users\tc_ca\AppData\Local\Programs\Python\Python312\python.exe` (not on PATH) | Installed. Runs `Tools/ue/*.py` in manifest (dry-run) mode. |
| Python (UE-embedded) | 3.11.8, ships inside UE 5.8 (`Engine\Binaries\ThirdParty\Python3\Win64`) | Present with the engine. Ran `Tools/ue/build_greybox.py` and `build_feel_gym.py` inside the editor on 2026-09-06 (section 1.1). |
| node | v24.14.0 | Installed. Runs `Tools/validate_floorplan.mjs`, `Tools/validate_blockout.mjs` (Gate 2), `Tools/check_manifest.mjs`, `Tools/glb_audit.mjs`, and the two data generators `Docs/candidates/legal_lead.build.mjs` (the floor) and `Docs/blockout/blockout.build.mjs` (the blockout). |
| git / git-lfs / gh | 2.53.0.windows.1 / 3.7.1 / 2.87.3 (authenticated as RobleusCaesar; scopes gist, read:org, repo, workflow) | Installed. |
| winget | 1.29.290 | Installed. |

**C++ state: compiled 2026-09-06** (`Tools/build_editor.ps1` = `Build.bat HellfallEditor Win64 Development`, MSVC 14.44 under UnrealBuildTool 5.8.2): `Result: Succeeded`, `Binaries\Win64\UnrealEditor-Hellfall.dll` linked (488,448 bytes), no other errors and no warnings from our module. The first real compile (after the NetFxSDK rules error above was fixed) produced exactly **one** error pair: `Source/Hellfall/HellfallTuning.cpp(324)` C2228 / C2665 - `const FKey Key(FName(*KeyName));` is parsed as a function declaration (the most vexing parse); fixed to `const FKey Key = FKey(FName(*KeyName));` in commit `49e9696`. The game target was then built by UAT inside `Tools/package.ps1` and the packaged binary boots (1.3). Before the compile the module had one source-level review against the installed 5.8 engine sources and four fixes from it (`Source/Hellfall/README-SOURCE.md`): the Enhanced Input mapping is built from the tuning subsystem in `SetupPlayerInputComponent`, so `Data/movement.json` binds and `invert_y` reach the mapping on first load and after every F1 toggle; `UHellfallMovementComponent` applies the tuning at `InitializeComponent` (spawn, before possession); both targets use `BuildSettingsVersion.V7` (= `Latest` in 5.8); focus regain while paused re-applies the paused input mode so alt-tab cannot lock the game. The remaining `// TODO(VERIFY 5.8): ...` markers (one in `Source/`: the mouse pitch sign, which needs a player's mouse) are listed in the current gate notes (`GATE-2-NOTES.md` section 6). **Editor-script state:** verified on 5.8.2, see 1.1. **Pipeline state:** the real project has been built, cooked, packaged and smoke-tested by `Tools/package.ps1 -Tag gate-0`, see 1.3 (1.2, the code-free twin, is the earlier proof).

### 1.1 First editor run (2026-09-06)

The level generators ran headlessly on the real engine without the C++ module, using a temporary code-free twin project `HellfallNoCode.uproject` (identical to `Hellfall.uproject` with the `Modules` array removed; git-ignored, deleted after use). Command, from the repository root:

```
"C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" C:\Users\tc_ca\projects\hellfall\HellfallNoCode.uproject -run=pythonscript -script=C:\Users\tc_ca\projects\hellfall\Tools\ue\build_greybox.py -stdout -FullStdOutLogOutput -unattended -nosplash -nopause -NoLiveCoding
```

Result (`Saved/Logs/hf_greybox_warmup.log.stdout.txt`, `grep LogPython`): `LogInit: Engine Version: 5.8.2-56702186+++UE5+Release-5.8`; `LogPython: Using Python 3.11.8`; `LogPython: HELLFALL greybox build: unreal / actors : 249 / warnings : none / OK`; `LogPythonScriptCommandlet: Display: Python script executed successfully`; `LogInit: Display: Success - 0 error(s), 0 warning(s)`; process exit code 0; about 32 s wall time. Output: `Content/Maps/L_ExecutiveFloor.umap` (486,684 bytes) and 24 `Content/Greybox/Materials/M_GB_*.uasset`. The same command with `build_feel_gym.py` (`Saved/Logs/hf_feelgym_warmup.log.stdout.txt`) wrote `Content/Maps/L_FeelGym.umap` (355,920 bytes): 171 actors, the same success lines, exit code 0.

What this proves: the `unreal` API calls in `Tools/ue/hf_common.py` work on 5.8.2 as written (level create/save, `StaticMeshActor` with `/Engine/BasicShapes/Cube`, one Material per tint, `TextRenderActor`, `PointLight` / `DirectionalLight` / `SkyLight`, `PlayerStart`); `sys.exit(0)` at the end of a `-run=pythonscript` commandlet exits the process with code 0; PythonScriptPlugin and EditorScriptingUtilities load in the commandlet. Not proven: non-zero exit propagation (`sys.exit(1)`); visual correctness (sky-light recapture, label facing) until the maps are opened. On the strength of this run the tools owner replaced five `TODO(VERIFY 5.8)` markers in `hf_common.py` with `VERIFIED 5.8.2` notes; the remaining markers are listed in `GATE-0-NOTES.md` section 6. Consequence: map generation is decoupled from the C++ build; `Tools/ue/run_editor_script.ps1 -NoCode` makes the twin-project run a supported mode (decision 3.20).

**Regenerated later on 2026-09-06 with the final generators** (even fill lights, single-sided labels facing the approach, non-colliding reference figures, and the level-recreate fix: `EditorAssetLibrary.delete_asset` returns True in the commandlet without deleting the `.umap`, after which `new_level` refuses the path, so `_recreate_level` now loads the existing map and clears its actors - both paths verified, create on the first run and `reusing /Game/Maps/L_ExecutiveFloor: cleared 249 actors before regenerating` on the second): `L_ExecutiveFloor.umap` 249 actors / **450,385 bytes**, `L_FeelGym.umap` 171 actors / **313,898 bytes**, 24 `M_GB_*` materials; 0 errors, 0 warnings; **committed in `12faf06`** (LFS 26 objects / 894 KB). These are the maps in the gate-0 zip (1.3). Byte-level determinism across regenerations has not been checked (`GATE-0-NOTES.md` section 6).

### 1.2 Pipeline proof (2026-09-06) - cook, stage, pak and archive on the code-free twin

To prove the packaging pipeline before the compiler exists, the lead ran UAT `BuildCookRun` (`-platform=Win64 -clientconfig=Development -cook -stage -pak -archive -nodebuginfo`) against a **code-free twin** of the project: `Saved/NoCodeTwin/HellfallNoCode.uproject`, the same JSON as `Hellfall.uproject` minus `Modules`, with directory junctions to the repository's `Content/` and `Config/`. The twin has to live in its own folder because UAT treats any folder containing `Source/` as a code project and would try to compile it. Result (`Saved/Logs/uat_pipeline_test2.log.stdout.txt`): **BUILD SUCCESSFUL, 0 errors, 176 s** (about 3 minutes); 596 MB staged - `UnrealGame.exe` 316 MB, `HellfallNoCode-Windows.ucas` 169 MB, `.pak` 10.6 MB, 1737 files - archived under `Builds/pipeline-test/` (git-ignored). The packaged `HellfallNoCode.exe` ran on this laptop's Vega iGPU under DX12 SM6 and loaded `L_ExecutiveFloor`. It uses the engine's default pawn because the `Hellfall` module is not compiled; the log line `Failed to find object 'Class /Script/Hellfall.HellfallGameMode'` is expected in that twin.

What this proves: the cook of both maps and the 24 greybox materials, the pak/iostore step, the staging layout and the archive step all work on 5.8.2 with this `Config/`, and the `DefaultEngine.ini` RHI choice (DX12, SM6) runs on the review machine. What it does not prove: anything about the C++ module, `Tools/package.ps1` itself (which adds `-build`, stages `Data/`, writes `Launch-FeelGym.cmd` and zips), or the gate-0 test in `GATE-0-NOTES.md` section 5 (first-person controller, F1, `Launch-FeelGym.cmd`) - none of that exists in a twin. **This was a pipeline proof only, not a gate-0 candidate; it is superseded by the real package in 1.3.**

### 1.3 gate-0 package (2026-09-06) - the compiled project, cooked, packaged and smoke-tested

`.\Tools\package.ps1 -Tag gate-0 -Configuration Development` on the compiled project at `49e9696`: UAT `BuildCookRun -platform=Win64 -clientconfig=Development -build -cook -stage -pak -archive -nodebuginfo` - **BUILD SUCCESSFUL** (`AutomationTool executed for 0h 3m 3s`; **211 s** wall time for the whole script as measured by the lead), log `Saved/Logs/package_gate0.log` (git-ignored). Game target built; both maps and the 24 greybox materials cooked; IoStore container `Hellfall-Windows` 1837 items, 171.54 MB -> 168.87 MB compressed (Oodle), `.pak` 1739 files / 11.1 MB; project shader library `Hellfall` 1563 unique shaders (SM6) / 1350 (SM5). Staged to `Builds\gate-0\Windows\`: `Hellfall.exe`, `Launch-FeelGym.cmd` (`start "" "%~dp0Hellfall.exe" /Game/Maps/L_FeelGym`), `BUILD-INFO.txt` (engine `5.8.2-56702186+++UE5+Release-5.8`, git revision `49e9696`, packaged 2026-09-06 16:53:11 UTC), `Data\` copied to `Builds\gate-0\Windows\Hellfall\Data`. Zip: **`Builds\Hellfall-gate-0-Win64-Development.zip`, 327.1 MB (342,939,354 bytes), SHA256 `76E2EC623A5B58D7CDB04D96AAA3813E081D38522D46D0FF7FA054E9156149A2`**; published as the `gate-0` Release with `GATE-0-NOTES.md` as its body: https://github.com/RobleusCaesar/hellfall/releases/tag/gate-0. `Builds\` is git-ignored; the zip ships only on the Release.

Smoke run: the packaged `Hellfall.exe`, windowed 1280x720 with `-DumpMovie` (plus `-benchmark -fps=5 -NoSound -unattended`, so a boot and rendering check, not a frame-rate measurement), once per map, on this laptop's Vega iGPU. Logs `Builds\gate-0\Windows\Hellfall\Saved\Logs\Hellfall.log` / `Hellfall_2.log`: `LogRHI: Using Default RHI: D3D12`, `Using Highest Feature Level of D3D12: SM6`; `LogLoad: Game class is 'HellfallGameMode'`; `LogHellfall: Tuning: loaded ...\Hellfall\Data\movement.json (walk 400 cm/s, stand 184 cm, 11 binds)` and `levels.json (2 levels)`; `Movement: tuning applied (walk 400 sprint 600 crouch 200 crawl 130 cm/s, jumpZ 432 cm/s, step 40 cm, transition 0.20 s)`; `Character: built 12 key mappings in C++ from Data/movement.json binds`; exactly one warning, expected and documented in `Docs/METRICS.md` row 5 (`crawl_height_cm 64 is below 2 * capsule_radius_cm (72); the capsule clamps to a 72 cm sphere`). Frames (`Builds\gate-0\Windows\Hellfall\Saved\Screenshots\Windows\MovieFrame*.png`): the HUD (title, CONTROLS block, STATUS block, centre dot); in `L_ExecutiveFloor` the Supply Closet with its room label, the amber `AIR DUCT 100 x 95 / crawl only` label and the NOTE labels all readable, even fill lighting with no blown-out ceiling, the duct mouth ahead; in `L_FeelGym` the row of stations with readable `CEILING 270 / 500 x 500 room, door 120` labels and the reference figures.

What this proves: REQ-G1-001 as far as a scripted run can - the pipeline is one command, the zip boots on the review machine to the first-person game mode with the data-driven tuning and input mapping in place, and the DX12 / SM6 choice runs on the iGPU. **What it does not prove** (no input in the smoke run; Rob's Gate 1 walk): movement feel, the duct crawl gate, collision integrity, stand-blocked behaviour, focus loss/regain, the F1 toggle, the data-only rebind, frame rate at 1920x1080 (G-7), byte-level `.umap` determinism. The full list is `GATE-0-NOTES.md` section 6; no Gate 1 acceptance criterion is claimed as met.

### 1.4 gate-1 package (2026-09-06) - the law-firm floor, textured greybox, enclosed feel gym

`.\Tools\package.ps1 -Tag gate-1 -Configuration Development` on the same pipeline as 1.3 (UAT `BuildCookRun -platform=Win64 -clientconfig=Development -build -cook -stage -pak -archive -nodebuginfo`, then `Data\` staged, `Launch-FeelGym.cmd` and `BUILD-INFO.txt` written, zip + SHA256): **`Builds\Hellfall-gate-1-Win64-Development.zip`, 327.6 MB (343,510,411 bytes), SHA256 `9B24B3B323B72F52C09C52A4C4C77A7AB608890E47F19F47744078A3D32D92B2`, 122 s wall time for the whole script.** Staged tree `Builds\gate-1\Windows\`: `Hellfall.exe` (a **bootstrapper** that starts `Hellfall\Binaries\Win64\Hellfall.exe` and exits at once - scripts that wait on or kill the game must target the inner binary, as `Tools/screenshot_sweep.ps1` does), `Launch-FeelGym.cmd`, `BUILD-INFO.txt` (tag gate-1, Development, engine `5.8.2-56702186+++UE5+Release-5.8`, git revision `e92bea2`), `Hellfall\Data\`, `Engine\`, `Hellfall\`. Cooked content: `L_ExecutiveFloor` (the 37-room law-firm floor, 590 actors), `L_FeelGym` (the enclosed hall, 222 actors) and 29 `M_GB_*` materials, 11 of them textured (the editor run reported every architectural tint under `materials_textured`, none under `materials_flat_fallback`). Published as the `gate-1` Release with `GATE-1-NOTES.md` as its body: https://github.com/RobleusCaesar/hellfall/releases/tag/gate-1. `Builds\` is git-ignored; the zip ships only on the Release. What the package changes against gate-0 is in section 3 (3.23-3.34) and the change log; what Rob is asked to test is `GATE-1-NOTES.md` section 3.

Review tooling that shipped with it (not gameplay): the map URL option `?spawn=X,Y,Z,Yaw` in `AHellfallGameMode` (Unreal cm and degrees; overrides the PlayerStart for one run), `Tools/screenshot_sweep.ps1` + `Data/review_shots.json` (31 viewpoints - 24 on the floor, 7 gym stations - each launched with `-DumpMovie`, last frame kept under `Saved\ReviewShots\<tag>\`), and `Tools/contact_sheet.ps1` (tiles the frames per map with captions so a sweep reads on a phone). The first gate-1 sweep is what exposed the light-radius defect fixed in 3.28.

### 1.5 gate-2 package (2026-09-07) - the blockout layer on the approved floor

`.\Tools\package.ps1 -Tag gate-2 -Configuration Development` on the same pipeline as 1.4 (the C++ module unchanged since gate-1; UAT `BuildCookRun -platform=Win64 -clientconfig=Development -build -cook -stage -pak -archive -nodebuginfo`, then `Data\` staged - now including `blockout.json` - `Launch-FeelGym.cmd` and `BUILD-INFO.txt` written, zip + SHA256): **`Builds\Hellfall-gate-2-Win64-Development.zip`, 327.9 MB (343,807,217 bytes), SHA256 `DCCF4C8999CA622FED6635F340772EB5B8D220743CDBAD78D7046719E940C8D3`, 164 s wall time for the whole script.** Staged tree `Builds\gate-2\Windows\` as at gate-1 (root `Hellfall.exe` bootstrapper, inner `Hellfall\Binaries\Win64\Hellfall.exe`). Cooked content: `L_ExecutiveFloor` = the approved 37-room floor **plus the Gate 2 blockout layer** in `Blockout/*` folders (192 props, 4 characters, 1 pickup + halo, 2 demon cylinders on their footprint slabs, 2 trigger volumes, the dwell volume, the backdrop plane, 203 labels; the dry run of the same inputs is 999 actors = 590 + 409, 513 collision boxes, 0 overlaps; `Content/Maps/L_ExecutiveFloor.umap` 1,744,821 bytes, regenerated 2026-09-07), `L_FeelGym` (222 actors, unchanged) and **42 `M_GB_*` materials** (29 from gate-1 + 13 blockout / volume / backdrop tints; 11 textured). Published as the `gate-2` Release with `GATE-2-NOTES.md` as its body: https://github.com/RobleusCaesar/hellfall/releases/tag/gate-2. `Builds\` is git-ignored; the zip ships only on the Release. What the package changes against gate-1 is section 3 (3.36-3.44), section 5 (the audit) and the change log; what Rob is asked to test is `GATE-2-NOTES.md` section 3. Not verified by the agent (Rob's Gate 2 walk): walking every route around the blockouts without catching (REQ-G2-002 AC3), whether rooms read occupied / crowded / empty, the pushed-out chair, the guard and shotgun being where you naturally look, backing away from Demon #1, the money shot with props in view, the survivors in the closet.

Review tooling for it: `Data/review_shots.json` grew to 41 viewpoints (10 prop-focused added); `Tools/screenshot_sweep.ps1 -Tag gate-2` + `Tools/contact_sheet.ps1 -Tag gate-2` write `Saved\ReviewShots\gate-2\`. The first gate-2 sweep is what exposed the volume wash over the money shot, the Demon #2 label inside the ceiling slab and the boundary slab in editor frames (3.40).

---

## 2. Review machine and the lighting-path decision (G-6, G-7)

Copied from `Docs/audit/environment-2026-09-05.md`. **This is the machine the agent runs on. Rob must confirm it is also the review machine**; if the review machine is a different, discrete-GPU PC, this section is rewritten and the lighting decision below is revisited before Gate 3.

| Item | Value |
|---|---|
| CPU | AMD Ryzen 7 7730U with Radeon Graphics, 8 cores / 16 threads (Zen 3, laptop U-class) |
| RAM | 15.4 GB usable |
| GPU | AMD Radeon (TM) Graphics - integrated Vega 8 class iGPU, 512 MB dedicated VRAM reported, shared system memory; driver 31.0.21924.1004 |
| Display | Samsung S27D390 external, 1920x1080 @ 60 Hz |
| Disk | C: 194 GB free of 456 GB; G: (Google Drive) 60 GB free of 100 GB |
| OS | Windows 11 Home 10.0.26200 |

**Decision (G-7 fallback, taken at Gate 0): no Lumen, no Nanite, no Virtual Shadow Maps. Static/baked lighting path, DX12 with Shader Model 6, TAA (not TSR), motion blur off.**

Rationale and citation: Epic's UE 5.8 hardware and software specifications state that Lumen Global Illumination and Reflections require an NVIDIA RTX 2000 series or newer, an AMD RX 6000 series or newer, or an Intel Arc GPU, and that Nanite requires Shader Model 6.6 with 64-bit atomics. A Vega-class integrated GPU meets neither, so the spec's default (Lumen, fall back if it misses 60 fps) is decided in advance rather than measured: Lumen would not run acceptably at 1920x1080 on this machine. Consequences:

- Gate 1 greybox lighting is even fill lighting (**Movable** non-inverse-square point lights, unitless intensity, no bake) with single-sided labels facing the player's approach (`Tools/README.md`), so regeneration needs no Lightmass pass and the map stays deterministic. Since 2026-09-06 the lights are a **grid per room, at most `light_spacing_cm` (800) apart on each axis, radius clamped to `min/max_attenuation_radius_cm` (800-1000)** - 59 lights on the law-firm floor, 48 hall lights + 12 station lights in the enclosed feel gym (decision 3.28); one light per room at 1.25 x the room diagonal gave the 44.8 m south corridor a 56 m light and blew the floor out to white in the first gate-1 screenshot sweep.
- From Gate 3, lighting is baked with CPU Lightmass (GPU Lightmass needs hardware ray tracing) plus stationary practicals for the flicker; reflections via reflection captures / screen-space; shadows via conventional shadow maps.
- `Config/DefaultEngine.ini` carries the project-wide switches for these choices (dynamic GI and reflection method set to none, Nanite disabled, virtual shadow maps disabled, anti-aliasing method TAA, motion blur off, DX12 default RHI with SM6 targeted). The ini is the authority; this paragraph describes intent.
- The 60 fps floor is measured on this machine with `stat fps` / `stat unit` along the critical path at every gate from Gate 1 on.
- **Gate 2 adds geometry, not lights:** the blockout layer is 409 more actors on the floor (197 collision boxes, 12 translucent / emissive markers, 203 text labels) under the same 59 fill lights; the volumes, halo and footprint slabs are translucent materials (opacity 0.1-0.3) and the backdrop an unlit emissive plane - the first translucent and emissive surfaces in the project, all engine `BasicShapes` with generated materials. The dry run is 999 actors (590 at gate-1). Frame rate with them is still unmeasured on the iGPU; if the packaged build stutters, the labels (203 TextRenders) are the first suspect and `labels.blockout` in `Data/greybox_style.json` the first knob.

---

## 3. Decisions the specification left open

Each entry: what was decided, why, and where it lives. All values are in `Data/` and can be changed without a code edit unless stated.

| # | Decision | Rationale | Where |
|---|---|---|---|
| 3.1 | **Project created from scratch** (text files: `Hellfall.uproject`, `Source/`, `Config/`), not from the First Person template. | The agent cannot drive the editor's template wizard, and the template ships Blueprints and `.uasset` input assets that G-3 forbids. Everything the template would have given us is rebuilt in C++. | `Hellfall.uproject`, `Source/Hellfall.Target.cs`, `Source/HellfallEditor.Target.cs`, `Source/Hellfall/` |
| 3.2 | **Binds:** Space = jump, Left Ctrl = crouch toggle, **C = crawl toggle**, Left Shift = sprint (hold), E = interact, Esc = pause, F1 = feel gym. Shown on screen by `AHellfallHUD`. | The spec lets the agent choose but notes Rob's earlier prototype used C for crawl; both earlier prototypes did, so C stays. Space is jump (not crouch) because the spec's controller section fixes Space as jump. Ctrl for crouch is the FPS convention. Rob still owns final acceptance (see `GATE-0-NOTES.md`). | `Data/movement.json : binds` |
| 3.3 | **Sprint is implemented** at 600 cm/s (hold). | Gate 5's 6 m retreat lanes and Gate 2's encounter validation are meaningless if the player cannot move faster than a walk; implementing it now lets the feel gym judge it. | `Data/movement.json : player.sprint_speed_cms` |
| 3.4 | **Capsule radius 36, standing height 184; eye height 166; crouch 116; crawl 64.** | Mid-range values that satisfy every derived rule with margin (door 112 <= 120, corridor 216 <= 280, duct 64 < 95 < 116). Full rationale per row in `Docs/METRICS.md`. | `Data/movement.json : player.*` |
| 3.5 | **Door 120 x 220; corridor 280; office ceiling 310; wet rooms 280; duct 100 x 95 x 500 long; wall 20.** | As specified, mid-range, or (ceiling 310) the lower third of the range; the executive floor's duct is built 520 face to face (4 % over 500, within the 5 % rule) so both rooms stay on the 50 cm grid; the wet-room ceiling is an addition so restrooms read differently from offices. See `Docs/METRICS.md`. | `Data/metrics.json : architecture.*` |
| 3.6 | **Walk 400, jump 95, coyote 0.10 s, step 40, acceleration 1500 / braking 1800, stance blend 0.20 s.** | Mid-range; the spec's "weight, not instant velocity" is the acceleration/braking pair. | `Data/movement.json : player.*` |
| 3.7 | **Three-stance movement is a custom `UHellfallMovementComponent`; the engine's built-in crouch is not used.** | Unreal has crouch but no prone. One component owning stand/crouch/crawl with a single clearance probe and a single interpolation path is simpler than mixing engine crouch with a custom crawl, and gives one place to suppress stand-up inside the duct (REQ-G1-006). | `Source/Hellfall/` |
| 3.8 | **Enhanced Input is set up entirely in C++** (Input Actions and the Mapping Context are created at startup from `Data/movement.json`); no input `.uasset` files. | G-3: binary input assets cannot be diffed. Building them in code makes a bind change a JSON edit. | `Source/Hellfall/`, `Data/movement.json : binds` |
| 3.9 | **`Data/` is staged into the packaged build by `Tools/package.ps1`** and read at runtime by `UHellfallTuning` (a `UGameInstanceSubsystem`). | REQ-G1-003 acceptance 3 ("change walk speed in /Data/ and rebuild changes in-game speed with no code edit") is met literally: the JSON is data, not compiled in. Rob can even edit the JSON next to the `.exe`. | `Tools/package.ps1`, `Source/Hellfall/` |
| 3.10 | **Plan-space coordinate convention** for `Data/floorplan.json`: x to the right of the photo, y down the photo toward the window; `UE.X = plan.y`, `UE.Y = -plan.x`, `UE.Z` up. | Lets the digitiser work in the drawing's own frame; a player at the CEO door facing +X looks straight at the window wall (checklist item 14) and keeps the drawing's handedness. | `Docs/FLOORPLAN-SCHEMA.md`, `Tools/ue/hf_geometry.py` |
| 3.11 | **Maps are generated, never hand-edited.** `/Game/Maps/L_ExecutiveFloor` from `build_greybox.py`, `/Game/Maps/L_FeelGym` from `build_feel_gym.py`; greybox materials under `/Game/Greybox/Materials`. Output is deterministic (sorted, no timestamps, no randomness). | G-3 and REQ-G1-005 acceptance 3 (regeneration reproduces the level identically). A `.umap` regenerated from unchanged inputs should produce an identical actor list; the manifest emitter proves that without the engine. | `Tools/ue/build_greybox.py`, `Tools/ue/build_feel_gym.py`, `Tools/ue/hf_common.py` |
| 3.12 | **Greybox lighting is even fill; labels are single-sided.** Movable point lights with inverse-square falloff off and a unitless intensity (no light bake in Gate 1; one per room at gate-0, a grid per room since 3.28); every label is one single-sided TextRender facing the player's approach. Details and the packaged-run evidence: `Tools/README.md`. | Baked lighting would make every regeneration a Lightmass run and would put a non-deterministic lightmap `.uasset` in LFS; Movable lights cost nothing at this scale. Even fill rather than inverse-square candela because the 2026-09-06 packaged-run frames showed inverse-square fixtures blowing the ceiling out while auto-exposure swallowed the change; single-sided because a TextRender reads only from its local +X side and the earlier back-to-back twin garbled the glyphs. | `Data/greybox_style.json`, `Tools/ue/hf_common.py`, `Tools/ue/hf_geometry.py` |
| 3.13 | **Feel gym access: F1 in game toggles between the two maps; `Launch-FeelGym.cmd` next to the executable starts the gym directly.** | REQ-G1-004 acceptance 2 (reachable without editing files) two ways: in-game and from a launch option. | `Data/movement.json : binds.toggle_feel_gym`, `Data/levels.json`, `Tools/package.ps1` |
| 3.14 | **Level registry** `Data/levels.json`: `executive_floor -> /Game/Maps/L_ExecutiveFloor` (order 10), `feel_gym -> /Game/Maps/L_FeelGym` (order 900, hidden). Each entry carries `id`, `display_name`, `map`, `order` and optional `hidden`. | Started now so the F1 toggle and Gate 6's level manager read the same file (REQ-G6-006); `UHellfallTuning` already loads it. | `Data/levels.json` |
| 3.15 | **Repository `RobleusCaesar/hellfall`, public, default branch `main`.** | The spec requires a public GitHub repository. Created 2026-09-06 by the lead with `gh` (authenticated as RobleusCaesar): origin = https://github.com/RobleusCaesar/hellfall, `main` pushed at the baseline commit `2c27652`, LFS push 123 objects / 113 MB; `main` is at `49e9696` since 2026-09-06 (`12faf06` fixes + maps, `7b3e60f` ignore `*.slnx`, `49e9696` compile fix), and the `gate-0` Release is published from it. | `.git`, `GATE-0-NOTES.md` |
| 3.16 | **Git LFS plan.** LFS tracks only (a) generated engine binaries under `Content/` and (b) the supplied bundle under `SourceAssets/`; nothing else binary enters the repo. Current LFS payload: `SourceAssets/` 109.9 MiB = 115.3 MB (reference images 42.2 MiB, models 23.3, textures 22.9, audio 20.1, video 1.4) plus the generated `.umap`/`.uasset` (about 1 MB at Gate 0: two maps and 24 greybox materials) - **about 115-120 MB**. Maps are committed at gate tags only, not per iteration, because every regenerated `.umap` is a new LFS object. | GitHub Free has historically included **1 GB of LFS storage and 1 GB of bandwidth per month**; GitHub has announced larger free quotas (10 GiB each) - the live figure is on the billing page and must be checked before Gate 2. **Every fresh clone pulls ~110 MB of LFS objects**, so on the 1 GB figure about nine clones a month exhaust bandwidth. Mitigations, in order: (1) clone with `GIT_LFS_SKIP_SMUDGE=1 git clone ...` then `git lfs pull --include="Content/**"` when only the playable content is needed; (2) sparse checkout excluding `SourceAssets/reference/` (38% of the payload and only for humans); (3) if a competing agent also clones this repository, or Gate 4 FBX exports push the payload past ~300 MB, buy one GitHub LFS data pack (50 GB storage + 50 GB bandwidth) or move `SourceAssets/` to a Release asset. The first push (2026-09-06) uploaded 123 LFS objects / 113 MB (126 tracked files; byte-identical duplicates share one object). Rob decides the quota plan before Gate 1 pushes. | `.gitattributes`, `.gitignore` |
| 3.17 | **Gate builds are Development configuration with `IncludeDebugFiles=False`** (no PDBs staged); the final is Shipping. | G-6 asks for Development at gates and Shipping at the end. Without PDBs a Development Windows build stays well under GitHub's 2 GB per-file Release limit (estimated 400-900 MB for a greybox project; gate-0 came in at 327.1 MB, see 1.3). If a later gate exceeds 2 GB the `.zip` is split, never committed. | `Tools/package.ps1` |
| 3.18 | **No Blender work before Gate 2.** The 17 GLBs are left exactly as supplied (wrong scale, see section 5); real-world rescale, pivots and collision happen in the scripted Blender pass at Gate 2/4. | G-1 and the gate protocol: Gate 1 is greybox only, and rescaling now would be work on unapproved scope. | `Docs/ASSET-MANIFEST.md` |
| 3.19 | **Lighting path** - see section 2. | | `Config/DefaultEngine.ini` |
| 3.20 | **Map generation runs without the C++ module (code-free twin).** `Tools/ue/run_editor_script.ps1 -NoCode` writes `HellfallNoCode.uproject` (= `Hellfall.uproject` minus `Modules`), runs the pythonscript commandlet against it and deletes it afterwards; the twin is git-ignored and `Hellfall.uproject` itself is never touched. | A C++ project whose module binary is missing stops at the editor's missing-module check, which cannot be satisfied without a compiler. The generators use only engine classes, so a code-free twin produces the same map. This decouples level iteration from compiler availability and proved the whole Python path on 5.8.2 the day the engine landed (section 1.1). `Binaries\Win64\UnrealEditor-Hellfall.dll` exists since the 2026-09-06 compile, so the normal project is used from now on; identical output is expected but has not been byte-compared (`GATE-0-NOTES.md` section 6). For UAT (cook/stage/pak) a twin cannot sit in the repository root, because UAT treats any folder containing `Source/` as a code project; the pipeline proof (1.2) therefore used a twin in its own folder, `Saved/NoCodeTwin/` (git-ignored), with junctions to `Content/` and `Config/`. | `Tools/ue/run_editor_script.ps1`, `.gitignore` |
| 3.21 | **`AndroidFileServer` plugin disabled in `Hellfall.uproject`.** | The project is Windows-only (G-6, `TargetPlatforms`), and with the plugin enabled the editor appended an `[/Script/AndroidFileServerEditor.AndroidFileServerRuntimeSettings]` block (with a generated `SecurityToken`) to the tracked `Config/DefaultEngine.ini` on every launch, contradicting the file's "every non-default explained" rule and churning the diff. Disabling it at the source stops the write; the block was removed from the ini and the header says why. | `Hellfall.uproject`, `Config/DefaultEngine.ini` |
| 3.22 | **Reference figures have no collision.** | They are scale references, not obstacles. Several stand 25-60 cm from walls (the corridor figure 25 cm off the south wall), and with collision on the REQ-G1-007 wall-slide would catch on them and Rob would report a defect that was by design. `reference_figure.collision` in `Data/greybox_style.json` records the choice; the lead applies it and regenerates. | `Data/greybox_style.json`, `Docs/METRICS.md` |
| 3.23 | **Rob's gate-0 feedback drives the gate-1 build; the spec's fixed adjacency stays the spine.** Rob reviewed the gate-0 screenshots (2026-09-06) and asked for: walls and a ceiling on the feel gym ("this is an internal map"); "an entire floor (not too big) of a legal office"; "a tiny amount of texture on floor, ceiling and walls"; "a bit of a maze ... where we can ultimately stage different monsters and scenes"; "about 10 minutes to explore and play". All five are treated as Gate 1 requirements on top of REQ-G1-001..007. | Change control: Gate 1 was **not yet approved** when the feedback arrived, so no approved scope was reopened - the gate-0 greybox was the first Gate 1 candidate, and Rob's notes on it are exactly the feedback loop the spec describes. The spec's required rooms and fixed adjacency (closet -> duct -> break room -> corridor -> restrooms / offices / collapse / elevator / reception -> CEO office with dividing wall and window wall) are unchanged and remain the critical path; the law-firm program is added around them. The 12-room gate-0 plan is preserved as `Data/candidates/floorplan_gate0.json` so the decision is reversible by a file swap. | `Docs/FLOORPLAN.md` sections 9-10, `Data/candidates/floorplan_gate0.json`, `GATE-1-NOTES.md` |
| 3.24 | **The law-firm floor is designed as a generator script, not as hand-written JSON.** `Docs/candidates/legal_lead.build.mjs` (room table `[id, label, x, y, w, h, ceiling, finish, enterable, role]`, one-line `door()` / `open()` / `locked()` calls, notes, checkpoints, encounters, scenes, money shot, critical path, with self-checks: 50 cm module, no overlaps, exactly one wall between neighbours, openings inside the shared extent) writes `Data/candidates/legal_lead.json`, promoted byte-identical to `Data/floorplan.json`. Program: 37 rooms (7 `corridor_*`, 2 lobbies `west_lobby` / `gallery_west`, 28 rooms), 38 openings, 1 duct, 2 blockers, 56 markers, 4 checkpoints, 2 encounters, 11 scenes; footprint 5500 x 4800 cm incl. walls; ring corridor around a service core (restrooms, stairwell, IT, janitor, copy/mail, records), two dead-end spurs south (boardroom / partner offices; conference / corner partner), a collapsed spur west, the paralegal bullpen as a shortcut through the core, the elevator lobby on the exterior east wall. | Three designer agents failed to emit the plan by hand (two hit the 64k-token single-response cap, then the account's usage limit): 37 rooms with 38 openings, 56 markers and 11 scenes is too much JSON to type reliably in one pass. A ~180-line script is reviewable as a table, refuses to write a broken plan, and turns every Rob edit into a one-line change plus a regenerate. Validator: PASS, exploration ~10.8 min, critical path ~103 m over 8 legs; dry run 590 actors, 316 collision boxes, 0 overlaps, enclosure 37/37, money shot 86.3 deg of 90 = 96 % from the CEO doorway (71 % net of the dividing wall, `Docs/FLOORPLAN.md` section 7). | `Docs/candidates/legal_lead.build.mjs`, `Docs/candidates/legal_lead.md`, `Data/floorplan.json`, `Docs/FLOORPLAN.md` |
| 3.25 | **Lobby widths: a transit room on the corridor -> reception leg counts as a corridor only when it is <= 320 wide; wider is a lobby, held to room rules.** `west_lobby` 980 x 580 and `gallery_west` 480 x 980 are lobbies (the validator prints an INFO line for each); the seven `corridor_*` rooms are 280 wide. Demon #1 must be in the room that Office #2's door opens into (a corridor or a transit lobby) - here `west_lobby`. | The spec puts Demon #1 "at the corridor near Office #2"; a 280 corridor T-junction is a poor fight space, so the two offices open onto a lobby that gives the fight a room (retreat 900 east, strafe 200) while the spec's adjacency (offices -> corridor space) still holds. The 320 threshold is the top of the spec's corridor range, so nothing wider can be mistaken for a corridor by the corridor rules. Whether 580 is deep enough is Rob's (`Docs/FLOORPLAN.md` Q7). | `Tools/validate_floorplan.mjs`, `Data/floorplan.json`, `Docs/FLOORPLAN.md` section 2 |
| 3.26 | **Surface texture: the engine's default grey grid, world-aligned, lerped faintly into each architectural tint.** `Data/greybox_style.json : surface_texture` - texture `/Engine/EngineMaterials/T_Default_Material_Grid_M` sampled through the engine function `/Engine/Functions/Engine_MaterialFunctions01/Texturing/WorldAlignedTexture`; base colour = `lerp(tint, grid, strength)`; per surface class floor 50 cm tile at 0.14, ceiling 60 cm at 0.10, wall 100 cm at 0.06, duct 25 cm at 0.10. Tints re-balanced so edges read: floors ~0.5, ceilings 0.72, walls ~0.85. Markers, glass, figures, the locked door, elevator doors and the collapse stay flat. Materials are rebuilt from the recipe on every editor run; if either engine asset fails to load the material is built flat and the run reports `materials_flat_fallback`. | Rob: "a tiny amount of texture on floor, ceiling and walls so it is easier to see the difference". Engine content only (G-4: nothing third-party, nothing to license); world-aligned so every scaled cube keeps one texel size; strengths low enough that Gate 3's real materials, not this grid, are what Rob judges. The 2026-09-06 editor run built all 11 architectural tints textured with no fallback. `surface_texture.enabled: false` returns every material to flat. | `Data/greybox_style.json`, `Tools/ue/hf_common.py`, `Tools/README.md` "Surface texture" |
| 3.27 | **The feel gym is an enclosed hall.** 20 cm walls around the 60 x 44 m slab, a ceiling at `metrics.feel_gym.hall_ceiling_cm` (450) over the whole slab (station rooms, corridors and tunnels keep their own lower ceilings inside it), no sun, no sky light; 48 even-fill hall lights on a grid no coarser than 800 cm plus the 12 station lights. | Rob: "add ceiling and walls, this is an internal map" - the gate-0 gym was an open slab under a black sky. 450 clears the tallest station top (350 room + 20 slab = 370) plus the 60 cm light drop, which the generator enforces. `check_manifest.mjs`'s enclosure check now holds the gym slab to the same rule as every room slab. | `Data/metrics.json : feel_gym.hall_ceiling_cm`, `Data/greybox_style.json : feel_gym`, `Tools/ue/build_feel_gym.py` |
| 3.28 | **Room lights are a grid, at most `light_spacing_cm` (800) apart on each axis, radius = clamp(1.25 x cell diagonal, 800, 1000).** 59 lights on the floor (37 rooms; the long corridors and the big rooms get several). | **Finding, first gate-1 screenshot sweep (2026-09-06):** with one light per room at 1.25 x the room diagonal, the 44.8 m `corridor_south` got a 56 m light, the lobby and the bullpen got 15-20 m lights, and the whole floor was blown out to white. The fix is a per-room grid with a hard radius cap, in `Data/greybox_style.json : lights` (`light_spacing_cm`, `max_attenuation_radius_cm`), not in a script; both maps were regenerated with it before packaging (the dry run shows the 59 lights and their clamped radii; the lead's re-sweep of the packaged build is the visual evidence, and Rob's walk the final word). Even fill stays (3.12); Gate 3 replaces all of it. | `Data/greybox_style.json : lights`, `Tools/ue/hf_geometry.py` |
| 3.29 | **Scenes: eleven reserved staging slots in five kinds (monster, ambush, scene, pickup, reveal), drawn as kind-tinted translucent floor markers with a `KIND: id` label and a wrapped description note; review markers only.** Note labels shrink from 16 to 11 cm and `labels.notes_enabled` switches every note-style label off for a clean screenshot pass. The validator WARNs below 8 scenes. | Rob: "design it as a bit of a maze ... where we can ultimately stage different monsters and scenes". Slots let Rob see where each beat is planned while he walks the greybox, without any gameplay; Gate 2 replaces them with real spawns, triggers and props. Kinds and colours are a style-table entry, so a sixth kind is a JSON edit. | `Data/floorplan.json : scenes`, `Docs/FLOORPLAN-SCHEMA.md` "Scene", `Data/greybox_style.json : scene_marker / labels`, `Docs/FLOORPLAN.md` section 6 |
| 3.30 | **Exploration estimate model** (`Data/metrics.json : exploration_model`, evaluated by the validator): corridors walked twice + each enterable non-corridor room (2 x shorter axis + 3 m) at walk speed + 12 s look-around per room, x 1.3 pacing; WARN outside 8-12 min, never a FAIL. The law-firm floor scores ~10.8 min; the gate-0 plan 3.1 min. | Rob: "about 10 minutes to explore and play". The earlier walking-only model (x 1.6, no look-around) gave ~4 min for the same floor because it ignores stopping at a door to read a room, which is most of a horror walk; the constants live in the metrics file so Rob's actual time can recalibrate them without touching the plan. The estimate is a design check, not a claim - Rob's walk is the referee (`GATE-1-NOTES.md` section 3). | `Data/metrics.json : exploration_model`, `Tools/validate_floorplan.mjs`, `Docs/FLOORPLAN.md` section 3 |
| 3.31 | **The declared critical path goes the long way round and is not enforced.** `supply_closet -> break_room -> corridor_main -> corridor_north_w -> west_lobby -> gallery_west -> corridor_south -> reception -> ceo_office`, ~103 m over 8 legs, 3 corridor junctions + 2 lobby junctions. The shortest route is ~63 m through the bullpen chord (78.6 m round the east corridor); both bypass Demon #1. `sc_junction_ambush` at the bullpen's south opening is the placeholder that guards them. | The long route carries Demon #1 and the collapse read; the bypasses are player freedom in a maze. Whether the west route must be forced (a blocker on the bullpen's south opening, a locked fire door at the east corridor's south end) is a design call that changes the maze Rob asked for, so it is left to him (`Docs/FLOORPLAN.md` Q2, `GATE-1-NOTES.md` section 5). | `Data/floorplan.json : critical_path`, `Docs/FLOORPLAN.md` section 2 |
| 3.32 | **Review tooling: `?spawn=X,Y,Z,Yaw` map option, screenshot sweep, contact sheet.** `AHellfallGameMode` reads the URL option and spawns the local player there (Unreal cm / degrees, capsule lifted automatically; absent or malformed = normal PlayerStart). `Tools/screenshot_sweep.ps1` launches the packaged game once per entry of `Data/review_shots.json` (31 viewpoints in plan space, converted `X = plan.y`, `Y = -plan.x`, `yaw = plan yaw - 90`) and keeps the last `-DumpMovie` frame; `Tools/contact_sheet.ps1` tiles them per map with captions. | Rob reviews from his phone; a captioned contact sheet of 31 fixed viewpoints is faster to read than a video and reproducible after every regeneration. The option is a review aid, not a gameplay feature, and does nothing unless passed on the command line. The sweep targets `Hellfall\Binaries\Win64\Hellfall.exe`, because the root `Hellfall.exe` is a bootstrapper that exits at once. | `Source/Hellfall/HellfallGameMode.h`, `Tools/screenshot_sweep.ps1`, `Tools/contact_sheet.ps1`, `Data/review_shots.json` |
| 3.33 | **Validator and manifest-check rules added for gate-1** (`Tools/validate_floorplan.mjs`): transit rooms <= 320 wide on the corridor -> reception leg are corridors, wider are lobbies (INFO, room rules); Demon #1 must be in the room Office #2's door opens into; scenes schema; exploration estimate, critical-path length and junction count on the PASS line, with the model constants read from `Data/metrics.json`. `Tools/check_manifest.mjs`: **enclosure check** - every floor slab needs a ceiling covering it and four solid sides; only authored openings may be gaps (a gap > 30 cm fails); proven negative by removing a wall (775 cm gap) and the gym ceiling. | Each rule encodes a gate-1 requirement so a regression fails the dry run instead of reaching Rob: the enclosure check is "walls and a ceiling" made mechanical; the lobby rule stops a 980-wide room from being held to corridor limits; the exploration line makes the 10-minute brief a number that is printed on every run. | `Tools/validate_floorplan.mjs`, `Tools/check_manifest.mjs`, `Tools/README.md` "Validators" |
| 3.35 | **Auto exposure re-enabled; fill lights toned down** (gate-1, after the second screenshot sweep): the packaged exposure had been manual (`r.DefaultFeature.AutoExposure=False`, chosen at gate 0 for deterministic review frames), so rooms lit by 2-3 overlapping grid lights rendered white while single-light rooms were fine. `Config/DefaultEngine.ini` now sets `r.DefaultFeature.AutoExposure=True`, `.Method=0` (histogram), `.Bias=0`; `Data/greybox_style.json` lights: default 4, role intensities x0.65, radius clamp 500-800 cm (= `light_spacing_cm`), `attenuation_diagonal_factor` 1.0. Rationale: a greybox review needs every room readable regardless of how many fill lights overlap; the Gate 3 horror lighting will want adaptive exposure anyway. Risk: exposure breathing between rooms (Rob to judge). |
| 3.34 | **Placement fixes after the floor-plan audit** (JSON / build-script only): the `west_lobby` figure moved to room origin + (880, 100) = (1380, 1550), out of the lobby -> corridor opening; the `corridor_sw` figure to origin + (140, 100) = (2240, 3450), out of `sc_spur_sw`'s rect; the `ceo_office` figure to origin + (100, 300) = (2500, 4300), out of Demon #2's trigger strip; `corridor_main`'s figure key fixed (it had fallen back into the opening between the two north corridors; now (4880, 1890)); `cp_ceo_entry` moved to (3140, 4040) and Demon #2's trigger strip to y 4100-4200 so a respawn does not refire the fight; `note_files` now describes 400 cm shelving stacks (the room is 480 deep); `sc_break_trail` moved to x 4600-4850, y 1150-1300, off the round table's footprint. | Found by the `Docs/FLOORPLAN.md` author's audit; none is checked by the validator (figures have no collision, markers are markers), so they are recorded here rather than left to a tool. | `Docs/candidates/legal_lead.build.mjs`, `Data/floorplan.json`, `Docs/FLOORPLAN.md` sections 5 and 11 |
| 3.36 | **Audit sizes: one real-world driving dimension per slot, applied as a UNIFORM scale; where a model cannot fill a frozen opening, height wins and the greybox fills the flanks.** The Blender audit (section 5, `Docs/audit/blender_audit_2026-09-07.md`) replaced the section 5 proposals that had been written without opening the files: desks 157 x 157 (chair + monitor baked in), kitchen set 160 x 114 with two chairs, vanity 246, wall-hung toilets 40 x 41 x 46, couch set one 280 x 254 x 80 L-mesh, closed door 102 x 17 x 220, elevator 134 x 21 x 220, guard a seated slump 78 x 161 x 82, demons 1.70 m in the file scaling 1.29x / 1.76x to 220 / 300. The door model (aspect 0.46) stands 102 wide in the 120 opening and the elevator model (0.61) 134 wide on the 200 blocker; the greybox jambs fill ~9 / ~33 cm each side. | Meshy textures smear under non-uniform scale, so a blockout box that will be swapped for the real mesh within 10 % (REQ-G4-002) must keep the model's proportions. Stretching the door to 120 or the elevator to 200 (327 tall, above the 310 ceiling) was rejected; resizing the openings would reopen approved Gate 1 scope. Rob's call on the surrounds is `GATE-2-NOTES.md` B7 / `Docs/blockout/BLOCKOUT.md` Q10. | `Docs/audit/blender_audit_2026-09-07.json` (`SLOTS` in `Tools/blender/audit_glb.py`), `Data/blockout.json : asset_slots` |
| 3.37 | **Guard body waiver (lead decision 2026-09-07): the dead security guard keeps the spec placement along `corridor_main`'s north wall (x 4300-4461, shotgun at his hand x 4470-4590) and a CHARACTER-only corridor rule, `blockout.rules.corridor_clear_min_past_body_cm` = 150, lets a body against a wall leave 150 instead of 216.** Past him the corridor keeps 202 of its 280. Props keep 216; both checkers apply the waiver only to stations narrowed by seated / prone / slumped characters with a wall contact, and print every waived station on the PASS line. | The audited mesh is a seated slump (legs 161 out), not the prone body `note_guard` assumed; along the wall it leaves 202 (legs across, 119), under the 216 minimum that protects walking speed past architecture. A first cut moved him into the break room, which emptied `sc_guard_shotgun` and the post-pickup checkpoint of their content and put the pickup where the corridor narrative (checklist 4) does not look. 150 = capsule diameter 72 + the 78 cm body: you step past a corpse, you do not slide along it. Gate 4 poses the mesh shoulder-to-the-wall or re-authors it prone; Rob may move him to the elevator alcove (`GATE-2-NOTES.md` B1). | `Data/greybox_style.json : blockout.rules`, `Docs/blockout/blockout.build.mjs`, `Tools/validate_blockout.mjs`, `Docs/blockout/BLOCKOUT.md` sections 1 and 7 |
| 3.38 | **Generic furniture for the law-firm rooms; corridors, lobby and gallery dressed.** The bundle covers the spec's 12 rooms; the 25 law-firm rooms of the approved floor are furnished from a generic kit of real-world office sizes (`KIT` in the build script: associate / partner desks, office / guest / executive chairs, bookcases, credenzas, filing cabinets, bullpen islands, library and file stacks, server racks, copiers, boardroom and conference tables, stall partitions, consoles, planters, water coolers, a bench, wall cabinets, a notice board, a cleaning cart) as `slot: null` boxes in the `blockout_generic` tint. 175 of the 192 props are kit; the corridors carry 3-4 wall items <= 50 deep each (clear widths 240 / 240 / 240 / 230), none in a swing zone, none opposite another at the same station, none in the guard beat (x 4300-4700) or the Demon #1 trigger span. | Checklist 1 ("occupied, crowded or empty") cannot be answered for 25 empty rooms; a kit with no asset behind it makes the composition reviewable now and leaves Gate 4 free to source CC0 meshes or keep them grey. "Empty by design" corridors were withdrawn because a 45 m bare corridor reads as a tunnel, not a hall. | `Docs/blockout/blockout.build.mjs` (`KIT`), `Docs/blockout/BLOCKOUT.md` sections 2-3 |
| 3.39 | **Tableau rule: a prop may lie inside a staging slot ONLY when the slot is kind `scene` and the prop's note names it - the prop IS the tableau; monster / ambush / pickup / reveal floors stay open; characters / pickups inside a slot are WARNs.** In force: the 600 x 160 boardroom table and five chairs in `sc_boardroom`, the middle bullpen island and four chairs in `sc_bullpen_massacre`; the guard and shotgun inside `sc_guard_shotgun` are its content. | A first cut evicted the tables to keep the markers visible and left 5 m of empty carpet where the interrupted meeting should be, plus a desk 10 cm past a swing zone. The interrupted boardroom and the massacre island ARE the scenes Rob asked to stage. | `Docs/FLOORPLAN-SCHEMA.md` "Blockout data", `Tools/validate_blockout.mjs`, `Docs/blockout/BLOCKOUT.md` section 7 (3) |
| 3.40 | **Volumes at opacity 0.1; blockout labels never inside a ceiling; the boundary shell hidden in the editor viewport too** (verifier findings on the first gate-2 sweep, 2026-09-07). `trigger_volume` / `dwell_volume` tints are the floor-marker colours at opacity 0.1 instead of 0.3, and `blockout.rules.sightline_volume_max_opacity` (0.1) makes the validator WARN if a volume in the money-shot strip above eye height names a denser tint. A label whose lifted text would enter the ceiling slab (Demon #2, 300 under 310) moves onto the item face at eye height (`labels_on_face`); `check_manifest.mjs` fails any room label whose text top reaches the ceiling. The seven boundary boxes get component `set_visibility(False)` as well as hidden-in-game (`TODO(VERIFY 5.8)` on the first live run: absent from the viewport, still blocking at the perimeter). | The demon_2 trigger stands 110 cm in front of the CEO doorway across the whole 90 deg FOV and the dwell volume against the glass, so at 0.3 the money shot was judged through a red / blue wash - REQ-G2-005 AC2 says no blockout may obstruct the sightline, and a wash is an obstruction. The Demon #2 label sat at z 315 inside the slab, invisible. The magenta `Boundary_south` slab stood between the glass and the backdrop in every editor frame taken without Game View (PIE and the packaged run were never affected). | `Data/greybox_style.json : tints / blockout`, `Tools/ue/hf_geometry.py`, `Tools/ue/hf_common.py`, `Tools/check_manifest.mjs`, `Tools/README.md` |
| 3.41 | **Closed-door leaves stand only in the two LOCKED openings (women's restroom, stairwell on `corridor_south`), flush on the corridor face inside each 120 span, 17 cm proud of the greybox LOCKED infill; the swing-zone rule is waived for a `door` prop inside a `locked_door` span.** Every other door - the critical-path break-room and CEO doors included - stays an open opening as approved at Gate 1; the burst-open CEO door is `SM_BrokenDoor` lying flat in reception 100 cm east of the doorway. | `SM_ClosedDoor` is a shut panel WITH casing, not a leaf: standing it beside an open doorway (the first cut's "swung-open leaves") reads as a second, fake door, and hinging it into an open doorway would block a critical-path route. In the locked spans it is judged where it will be used (the Gate 5 refusal doors), and 263 of the 280 stay clear. Whether the infill should be cut back at Gate 3 so the mesh sits in the wall plane, and whether closed leaves should also stand on the critical-path doors, are Rob's (`GATE-2-NOTES.md` B5, B6). | `Docs/blockout/blockout.build.mjs`, `Docs/blockout/BLOCKOUT.md` section 7 (4) |
| 3.42 | **The CEO desk stands WEST of the door -> window axis** at the approved marker `note_ceo_desk` (2900, 4400), yawed 15 deg toward the glass, with the two guest chairs west of it and a side console on the east wall north of the trigger strip - not east as the Gate 2 brief said. | East of the axis, north of the dividing wall is the Demon #2 trigger strip and strafe lane and south of it the demon's pocket; the approved Gate 1 note already put the desk west. Chairs in front of the desk would sit in the retreat lane. Rob's call: `GATE-2-NOTES.md` B2. | `Data/blockout.json` (`ceo_desk`), `Docs/blockout/BLOCKOUT.md` section 5 |
| 3.43 | **Backdrop: a flat emissive plane 12000 x 5000, bottom at z -2500, 800 cm beyond the glass (y 5600), centred on the window, outside the boundary shell**, `backdrop_fire` tint (emissive 3 = unlit glow), no collision. Checked from the doorway AND from every dwell-rect corner at eye height with the 90 deg FOV pitched a full 16:9 vertical FOV (58.7 deg) down or up (needs x 1870-4410, z -2092..2424: covered). | REQ-G2-005 allows "a flat orange-lit plane or a simple skybox". A 4000 x 1600 plane at z -300 covered the glass from the doorway only; from the dwell rect looking 15 deg down showed the void under it. Rays through the glass edges from the dwell corners reach x -3285..9565, past the plane - a grazing look along the glass finds its edge; plane vs sky sphere is Rob's (`GATE-2-NOTES.md` B4). Gate 3 replaces it with the sky / city card. | `Data/blockout.json : backdrop`, `Tools/validate_blockout.mjs`, `Docs/blockout/BLOCKOUT.md` section 5 |
| 3.44 | **Placement fixes and review additions from the blockout verifier pass** (data / script only): the seated man moved to a NORTH rack section (the racks form an L: a 100 x 60 north section against the full-length east rack) so his 144 cm legs clear the start -> duct route by 27 cm (with his back on the east rack they crossed it) - and the start -> duct capsule sweep is now a rule in both checkers; the reception desk carries a 30 x 10 x 5 "FRONT" floor plate marking the corridor side (the chair is inside the mesh, south); each demon cylinder stands on a translucent no-collision footprint slab of the audited wing span (162 x 204 / 208 x 83 x 10) facing the player approach, so REQ-G2-003 AC1 is visible without a rule counting it; the mop set stands in the supply closet (its manifest room) and the vanity in the men's room; the backdrop is checked from the dwell corners (3.43); 10 prop-focused viewpoints added to `Data/review_shots.json` (41). New checks in `Tools/validate_blockout.mjs`: schema + Blender-audit cross-check of `asset_slots`, asset coverage, oriented-footprint placement rules, the widest spawn -> approach route per encounter (demon_1 450, demon_2 310) and the dividing-wall chokepoint (308.9 >= 280), the pickup-scene content check; `Tools/check_manifest.mjs` gained the blockout containment / cylinder / backdrop checks and the label-under-ceiling check; `Tools/test_dry_run.ps1` steps 6a-6d (14 steps). | Found by the verifier's review of the first blockout cut and of the first gate-2 screenshot sweep; each is either a rule now (so it cannot regress) or a marker with no gameplay. | `Docs/blockout/blockout.build.mjs`, `Data/blockout.json`, `Tools/validate_blockout.mjs`, `Tools/check_manifest.mjs`, `Tools/test_dry_run.ps1`, `Data/review_shots.json`, `Docs/blockout/BLOCKOUT.md` section 7 |

---

## 4. Blueprints

G-3 permits Blueprints only where the engine gives no text alternative, and requires every one to be listed here with its purpose and a prose description of its logic.

| Blueprint asset | Purpose | Logic (prose) |
|---|---|---|
| *(none)* | | |

**Statement:** as of Gate 0 the project contains **zero Blueprint assets**. Gameplay, input, HUD and the game mode are C++; levels are Python-generated. The first Blueprints expected are Animation Blueprints for the two demons at Gate 4/5 (or direct montage playback from C++ if that proves sufficient); they will be added to this table when they exist.

---

## 5. Asset working set (REQ-G2-001 - audited in Blender, 2026-09-07)

**Status: REQ-G2-001 done.** Every GLB was opened headlessly in Blender 5.2.1 (`Tools/blender/audit_glb.py`; exit code 0, 17 files, 0 errors), measured in rest pose, cross-checked against the node header parser (`Tools/glb_audit.mjs` -> `Docs/audit/glb_audit.jsonl`) and given its ruling. Full audit: `Docs/audit/blender_audit_2026-09-07.md` (+ `.json`, whose `asset_slots` block, keyed by Unreal name, is what `Data/blockout.json` copies verbatim into its `asset_slots`: `normalized_size_m` / `tris` ARE the Blender values, Z-up W x D x H; the parser's view rides along as `parser_size_m` / `tris_parser`, record only). The complete per-file list (all 126 files) is `Docs/ASSET-MANIFEST.md`; where every asset's blockout stands is `Docs/blockout/BLOCKOUT.md` (section 6, asset slots -> blockout id and room).

**Provenance.** `SourceAssets/` is already the de-duplicated working set from Rob's Godot attempt (`RobleusCaesar/hellfallGPT`): one candidate per slot, no ` - Copy` files, `Meshy_AI_*` prefixes and the `deamon` misspelling already renamed, GLBs decimated with glTF-Transform 4.5.0. The raw 2.8 GB Meshy bundle (with `shotgun2`, `ceo_dead2`, the third demon) is not on this machine, so "rejected - reason" rows cannot be produced for it; the Godot-era selection is inherited.

**Scale as supplied.** Every static GLB is normalised so its longest axis is ~1.9 m, whichever axis that is (doors, fridge, elevator, mop set and toilet are 1.9 tall; shotgun, vanity, desk and couch set are 1.9 long; the corpses 1.9 along the body), with the bounding-box centre at the origin. **The two rigged demons are 1.70 m tall with their feet at z = 0** - the earlier "0.017 m, about 100x too small" was the header parser applying the armature's 0.01 scale to skinned bind buffers that glTF says to ignore; Blender's skinned rest-pose measurement is authoritative (scale factors to target 1.29x and 1.76x, not 130x / 176x). Targets below are **one real-world driving dimension per slot applied as a uniform scale** (Meshy textures smear under non-uniform scale), so width, depth and height keep each model's proportions. Blender's triangle count (after its mesh validation drops the duplicate faces glTF-Transform's decimation left behind) is the recorded count; the parser's pre-import count stands beside it.

### 5.1 Meshes (17) - audited

Axes are Blender Z-up after the glTF importer's Y-up conversion: W along x, D along y, H up. Every slot is kept.

| Source file | Unreal name | Tris (Blender / parser) | Measured rest size W x D x H (m) | Rig / animation clips | Real-world target (cm) - **audited 2026-09-07** | Driving dimension | Pivot | Gate 4 class budget | Rejected / duplicate |
|---|---|---:|---|---|---|---|---|---|---|
| `models/props/reception_desk.glb` | **SM_ReceptionDesk** | 7,796 / 7,798 | 1.896 x 1.888 x 1.330 | static | 157 W x 157 D x 110 H | work surface at 75 | floor | Large furniture <= 8,000: OK | - |
| `models/props/kitchen_lunch_table.glb` | **SM_KitchenLunchTable** | 7,803 / 7,806 | 1.899 x 1.349 x 1.063 | static | 160 W x 114 D x 89 H | work surface at 75 | floor | Large furniture <= 8,000: OK | - |
| `models/props/refrigerator_open.glb` | **SM_RefrigeratorOpen** | 7,789 / 7,792 | 0.919 x 1.303 x 1.902 | static | 87 W x 123 D x 180 H | height 180 | floor | Large furniture <= 8,000: OK | - |
| `models/props/bathroom_vanity.glb` | **SM_BathroomVanity** | 7,792 / 7,794 | 1.903 x 0.418 x 0.854 | static | 246 W x 54 D x 110 H | work surface at 85 | floor | Large furniture <= 8,000: OK | - |
| `models/props/toilet_bowl.glb` | **SM_ToiletBowl** | 3,798 / 3,798 | 1.634 x 1.685 x 1.896 | static | 40 W x 41 D x 46 H | length (x) 40 | wall | Small prop / door / fixture <= 4,000: OK | - |
| `models/props/mop_and_bucket.glb` | **SM_MopAndBucket** | 3,791 / 3,791 | 1.644 x 1.267 x 1.893 | static | 130 W x 100 D x 150 H | height 150 | floor | Small prop / door / fixture <= 4,000: OK | - |
| `models/props/ceo_couch_coffee_table.glb` | **SM_CeoCouchCoffeeTable** | 8,104 / 8,126 | 1.901 x 1.721 x 0.543 | static | 280 W x 254 D x 80 H | height 80 | floor | Large furniture <= 8,000: 1.3% over (tolerance 20%) | - |
| `models/props/closed_door.glb` | **SM_ClosedDoor** | 3,805 / 3,806 | 0.882 x 0.145 x 1.903 | static | 102 W x 17 D x 220 H | height 220 | wall | Small prop / door / fixture <= 4,000: OK | - |
| `models/props/broken_door.glb` | **SM_BrokenDoor** | 4,000 / 4,012 | 1.902 x 0.986 x 0.107 | static | 205 W x 106 D x 12 H | length (x) 205 | floor | Small prop / door / fixture <= 4,000: OK | - |
| `models/props/closed_elevator.glb` | **SM_ClosedElevator** | 3,790 / 3,790 | 1.163 x 0.178 x 1.903 | static | 134 W x 21 D x 220 H | height 220 | wall | Small prop / door / fixture <= 4,000: OK | - |
| `models/weapons/shotgun.glb` | **SM_Shotgun** | 23,816 / 23,818 | 1.902 x 0.099 x 0.355 | static | 120 W x 6 D x 22 H | length (x) 120 | muzzle | Hero weapon <= 25,000: OK | - |
| `models/characters/man_sitting.glb` | **SM_ManSitting** | 9,502 / 9,506 | 1.284 x 1.905 x 1.128 | static (posed) | 97 W x 144 D x 85 H | height 85 | floor | Human prop <= 10,000: OK | - |
| `models/characters/intern_sitting.glb` | **SM_InternSitting** | 9,185 / 9,846 | 1.041 x 1.784 x 1.900 | static (posed) | 49 W x 85 D x 90 H | height 90 | floor | Human prop <= 10,000: OK | - |
| `models/characters/fallen_security_guard.glb` | **SM_FallenSecurityGuard** | 9,479 / 9,485 | 0.918 x 1.902 x 0.971 | static (posed) | 78 W x 161 D x 82 H | height 82 | floor | Human prop <= 10,000: OK | - |
| `models/characters/ceo_dead.glb` | **SM_CeoDead** | 9,477 / 9,479 | 1.167 x 1.897 x 0.538 | static (posed) | 105 W x 170 D x 48 H | length (y) 170 | floor | Human prop <= 10,000: OK | - |
| `models/characters/ember_demon.glb` | **SK_EmberDemon** | 10,418 / 10,418 | 1.253 x 1.575 x 1.700 | 1 skin, 24 joints; clips: Attack, Hit_Reaction, Idle_8, Shot_and_Fall_Backward, Walking | 162 W x 204 D x 220 H | height 220 | floor | Enemy <= 15,000: OK | - |
| `models/characters/crimson_hellfiend.glb` | **SK_CrimsonHellfiend** | 10,428 / 10,428 | 1.177 x 0.468 x 1.700 | 1 skin, 24 joints; clips: dying_backwards, Hit_Reaction_1, Idle_5, Idle_8, Right_Hand_Sword_Slash, Running, Shield_Push_Left, Simple_Kick, Walking, walking_2_inplace | 208 W x 83 D x 300 H | height 300 | floor | Enemy <= 15,000: OK | `crimson_hellfiend_texture_0_1.png` rejected - byte-identical duplicate of `_texture_0.png` |

**What the orthographic previews showed** (the reasons behind the odd numbers): `toilet_bowl` is a wall-hung bowl with exposed plumbing - drain down and back into the wall, flush valve on top, no tank, no pedestal - hence the near-cubic bounds and the wall pivot (box bottom ~2 cm above the floor puts the rim at ~42); `fallen_security_guard` is a **seated slump against a wall with legs straight out (161 cm), not a prone body** - along the 280 corridor he would leave ~120 clear (legs across) or 202 (back on an end wall), both < the 216 minimum, so the Gate 2 blockout seats him INSIDE the break room against its west wall 2 m north of the corridor door (`Data/blockout.json` `guard`, labelled "MOVED from the corridor"), a deviation from REQ-G2-003 / `note_guard` / `sc_guard_shotgun` put to Rob as `Docs/blockout/BLOCKOUT.md` Q1 with two alternatives (corridor end wall with a 216 waiver; the elevator alcove) - or the pose is re-authored at Gate 4 (the plan's `note_guard` assumed a prone body); `reception_desk` is one mesh with the office chair, monitor, cup and a fallen item inside a 157 x 157 footprint, and the chair back rising above the work surface makes it desk height (75), not a standing counter; `kitchen_lunch_table` has two chairs, not four; `mop_and_bucket` includes a wet-floor A-frame sign; the couch set is an L (3-seat sofa, perpendicular loveseat, coffee table in front of the sofa). The door model (aspect 0.46) and elevator model (0.61) cannot fill the frozen 120 x 220 / 200 x 220 openings at uniform scale - height wins and the greybox fills the flanks (~9 cm and ~33 cm each side).

**Rigs.** Both demons carry one armature (object scale 0.01), 24 bones rooted at `Hips` (Mixamo-style names), one deformed mesh `char1` with 24 vertex groups, and named actions with real frame ranges at 24 fps (ember: Attack 2.8 s, Hit_Reaction 1.6, Idle_8 8.0, Shot_and_Fall_Backward 3.5, Walking 1.0; crimson: dying_backwards 2.2, Hit_Reaction_1 1.2, Idle_5 1.9, Idle_8 8.0, Right_Hand_Sword_Slash 1.5, Running 0.6, Shield_Push_Left 2.4, Simple_Kick 2.3, Walking 1.0, walking_2_inplace 1.2). Gate 5 mapping unchanged: ember Idle_8 -> Idle, Walking -> Chase, Attack -> Attack, Hit_Reaction -> flinch, Shot_and_Fall_Backward -> Death; crimson Idle_5 / Idle_8 -> Idle, Walking / Running -> Chase, Right_Hand_Sword_Slash + Simple_Kick -> Attack (two variants), Hit_Reaction_1 -> flinch, dying_backwards -> Death, walking_2_inplace if root motion is stripped. Whether the skinning survives the FBX round-trip is still the Gate 4 question (REQ-G4-004 acceptance 2). The Blender importer adds an 80-triangle `Icosphere` bone-shape widget that is not in the GLB; the audit excludes it and the Gate 4 export must skip it.

**Rejected / duplicate.** `models/characters/crimson_hellfiend_texture_0_1.png` - **rejected, byte-identical duplicate** of `crimson_hellfiend_texture_0.png` (same MD5; the GLB embeds both `texture_0` and `texture_0.001` at 1024 x 1024). **Still on disk as of 2026-09-07** - the lead removes it with `git rm` (LFS-tracked) in the Gate 2 commit; until then REQ-G2-001's "rejected files are removed from the working set" is documented, not done (acceptance 2 - no forbidden names - is met). The ` - Copy` / `deamon` / `Meshy_AI_*` / `shotgun2` / `ceo_dead2` / third-demon rows the spec expects were resolved by the Godot-era pass and are not on this machine (Provenance above); `SourceAssets/` contains none of those names (acceptance 2 re-verified 2026-09-07).

**Cross-check against the parser.** Triangles agree within 1 % for 16 / 17 files, and every difference equals the duplicate-face count in that file's index buffer (a second triangle over the same three vertices, either winding - Blender's mesh validation removes them): 0-22 per file, 661 in `intern_sitting` (6.7 %), the flipped duplicates that fake two-sided hair cards - give the hair a two-sided material at Gate 4. Bounds agree within 1 % for all 15 static meshes (worst 0.28 %, `closed_door`); the skinned demons differ by the parser's factor ~100 and agree in axis order. Vertex counts are identical for all 17.

### 5.2 Audio (14 files - the earlier brief said 16; the tree holds 14)

`deamon` -> `demon` is already normalised in every name. Sound Class assignments are the Gate 6 plan (REQ-G6-002).

| Source file | Proposed name | Format | Size | Use |
|---|---|---|---:|---|
| `audio/music/under_broken_steel.mp3` | MUS_UnderBrokenSteel | MP3 | 727 KB | Seamless loop under traversal (G3-004 / G6-002); class Music |
| `audio/sfx/city_fire_truck.wav` | SFX_CityFireTruck | WAV | 1.1 MB | Exterior ambience beyond the window; class Ambient |
| `audio/sfx/city_riot.wav` | SFX_CityRiot | WAV | 12.0 MB | Exterior ambience, burning city; class Ambient (largest audio file - candidate for a shorter loop) |
| `audio/sfx/demon_attack.mp3` | SFX_DemonAttack | MP3 | 54 KB | Melee attack (G5-004); class SFX, 3D |
| `audio/sfx/demon_attack_2.wav` | SFX_DemonAttack2 | WAV | 2.2 MB | Melee attack variant (the spec's `demon_attack2`); class SFX, 3D |
| `audio/sfx/demon_growl.wav` | SFX_DemonGrowl | WAV | 1.2 MB | Detection growl (G5-004); class SFX, 3D |
| `audio/sfx/demon_growl_distant.mp3` | SFX_DemonGrowlDistant | MP3 | 48 KB | Distant vocalisation, 20-60 s intervals (G3-004); class Ambient, 3D |
| `audio/sfx/monster_screech_distant.wav` | SFX_MonsterScreechDistant | WAV | 1.8 MB | Distant vocalisation, never overlapping the growl (G3-004); class Ambient, 3D |
| `audio/sfx/shotgun_blast.wav` | SFX_ShotgunBlast | WAV | 472 KB | Fire (G5-002); class SFX |
| `audio/sfx/shotgun_cocking.wav` | SFX_ShotgunCocking | WAV | 563 KB | Pump, exactly once per shot (G5-002); class SFX |
| `audio/sfx/shotgun_reloading.wav` | SFX_ShotgunReloading | WAV | 80 KB | Reload; the reload duration is this clip's length (G5-002); class SFX |
| `audio/voices/demon_i_am_death.mp3` | VO_DemonIAmDeath | MP3 | 88 KB | Optional voice line, at most once (G5-004); class SFX, 3D |
| `audio/voices/demon_i_am_the_darkness.mp3` | VO_DemonIAmTheDarkness | MP3 | 46 KB | Optional voice line, at most once; class SFX, 3D |
| `audio/voices/demon_i_will_kill_you.mp3` | VO_DemonIWillKillYou | MP3 | 35 KB | Optional voice line, at most once; class SFX, 3D |

WAV imports natively. MP3 import in UE 5.8 is expected but unverified on this machine; fallback is a WAV transcode at Gate 3/6. `// TODO(VERIFY 5.8): MP3 import.`

### 5.3 Textures, video, reference

| Group | Files | Proposed names | Notes |
|---|---|---|---|
| Per-model textures beside each GLB | 59 JPG/PNG (`*_base_color`, `*_metallic_roughness`, `*_normal`, `*_emissive`, `*_texture_0`) | `T_<Asset>_BC / _MR / _N / _E / _D0` | Loose copies of the images embedded in the GLBs. `crimson_hellfiend_texture_0_1.png` is byte-identical to `crimson_hellfiend_texture_0.png` (same MD5) - drop at Gate 2. |
| `textures/decals/` | `blood_pool.jpg`, `blood_splatter_01.jpg`, `blood_splatter_02.jpg` | T_Decal_BloodPool, T_Decal_BloodSplatter01/02 | REQ-G3-003: blood is Decal Actors, never geometry. The spec expected blood *meshes* to extract textures from; the working set already holds textures. |
| `textures/paintings/` | `wall_painting_01..05.png` | T_WallPainting01..05 | Wall dressing, Gate 3/4. |
| `textures/ui/` | `main_menu_background.png`, `hellfall_logo_reference.png` | T_UI_MainMenuBackground, T_UI_LogoReference | REQ-G6-001 main menu. |
| `video/` | `intro.ogv`, `outro.ogv` (713 / 734 KB) | MS_Intro, MS_Outro (FileMediaSource) | **Must be re-encoded to MP4/H.264 + AAC for the UE Media Framework at Gate 6** (`ffmpeg -i intro.ogv -c:v libx264 -pix_fmt yuv420p -c:a aac intro.mp4`). Placeholders per G-8. |
| `reference/` | 24 images: `game_layout.jpg` (the hand-drawn plan), `mood_before_1..4.png`, `mood_after_1..4.png` (the spec's `Before_4.png` / `After_4.png`), `base_image_*` and `base_model_*` Meshy inputs, `shotgun_reference.png`, `base_image_hands.png` | none | Never imported. |

---

## 6. Third-party and licence table (G-4)

### Shipped in or with the build

| Item | Source | Version | Licence | Used for |
|---|---|---|---|---|
| Unreal Engine, including `/Engine/BasicShapes` (Cube, Plane, Cylinder, Sphere, Cone), engine default materials and the engine's default TextRender font | Epic Games, via the Epic Games Launcher | 5.8.x (exact hotfix recorded in section 1 at install) | Unreal Engine EULA (ships with the engine; royalty terms per the EULA) | Engine; greybox primitives, reference figures and floating labels |
| Supplied asset bundle `SourceAssets/` (17 models with textures, 14 audio files, 3 decal textures, 5 paintings, 2 UI images, 2 videos, 24 reference images) | Owner-supplied by Rob. Models generated with Meshy.ai under Rob's account; audio/video/UI/mood-board provenance to be confirmed by Rob | n/a | Owner-supplied; Meshy output usable under the terms of Rob's Meshy subscription | All game content from Gate 2 on |
| *(no other third-party content yet)* | | | | Future CC0 sources for Gate 3 textures: ambientCG, Poly Haven (G-4). Fab assets only after per-asset licence check. |

### Tools (not shipped)

| Tool | Licence | Note |
|---|---|---|
| Blender 5.2.1 LTS | GPL-2.0-or-later | Tool only; its output carries no licence obligation. |
| Python 3.12 / 3.11 | PSF Licence | Tooling and editor scripting. |
| node 24 | MIT | Validators. |
| git, git-lfs, gh | GPL-2.0 / MIT / MIT | Repository operations. |
| Visual Studio 2022 Build Tools, Windows SDK, .NET Framework 4.8.1 Developer Pack, .NET SDK 8 | Microsoft licence terms (Build Tools are licensed for building open-source or with a VS licence; UE compiles under the Community/Build Tools terms) | Compiler and the SDKs UnrealBuildTool requires. |
| winget | MIT | Installer. |

---

## 7. Change log

| Date | Gate | Entry |
|---|---|---|
| 2026-09-05 | 0 | Scaffold created: C++ project skeleton (`Hellfall.uproject`, targets, `Hellfall` module with character, movement component, controller, game mode, HUD, tuning subsystem), Python level generators with a no-engine manifest mode, node validators, `Data/*.json`, documentation, `.gitignore` / `.gitattributes`. Machine audited; UE 5.8 and the C++ toolchain found **not installed**; G-7 lighting fallback decided in advance (section 2). Nothing compiled or packaged; **no `gate-0` Release yet** - blocked on Rob's Epic sign-in and an Administrator prompt (`GATE-0-NOTES.md`). Asset bundle audited (`Docs/audit/glb_audit.jsonl`, `Docs/ASSET-MANIFEST.md`). Baseline commit `2c27652`. |
| 2026-09-06 | 0 | **UE 5.8.2 (CL 56702186) installed** via the Epic Games Launcher; exact version recorded in section 1 (registry key not yet written by the Launcher - default-folder / `UE_ROOT` resolution documented). Blender 5.2.1 and .NET SDK 8.0.424 confirmed present (the 2026-09-05 "not installed" entries for .NET were wrong: 8.0.424 was already there); VS 2022 Build Tools installing. **First editor run** (1.1): both maps generated headlessly through the code-free twin project - 249 + 171 actors, 0 errors, 0 warnings; decision 3.20 added. C++ still uncompiled; **no `gate-0` Release yet**. Documentation review applied: validator usage with arguments, LFS sizes in MiB, registry field list, candidates A/B/C, `ModelingToolsEditorMode` in the plugin list, `TODO(VERIFY 5.8)` list regenerated from grep. |
| 2026-09-06 | 0 | **GitHub remote created and pushed** (https://github.com/RobleusCaesar/hellfall, public; `main` at `2c27652`; LFS 123 objects / 113 MB). **Pipeline proof** (1.2): UAT BuildCookRun of the code-free twin in `Saved/NoCodeTwin/` - BUILD SUCCESSFUL, 0 errors, 3 min, 596 MB staged; the packaged twin ran on the iGPU under DX12 SM6 and loaded `L_ExecutiveFloor` (default pawn; not a gate-0 candidate). **Build Tools still not installed**: two winget attempts cancelled at the UAC prompt (exit 1602); Rob given the elevated one-liner and the manual installer route (`Docs/TOOLCHAIN-SETUP.md` step 2). **C++ review fixes** (uncompiled): input mapping built from the tuning subsystem (JSON binds / `invert_y` now reach the mapping), tuning applied at `InitializeComponent`, `BuildSettingsVersion.V7`, paused input mode re-applied on focus regain. **Greybox rework** (tools owner): even fill lights and single-sided labels facing the approach (3.12, `Tools/README.md`); reference figures non-colliding (3.22). `AndroidFileServer` disabled in the `.uproject` and its ini block removed (3.21). HUD header and `DefaultGame.ini` description no longer say "Gate 1". `Docs/METRICS.md`: duct mouth lips (clear 100 x 83), literal FOV. Completeness review applied; `TODO(VERIFY 5.8)` list regenerated. Still **no `gate-0` Release** and no compiled module. |
| 2026-09-06 | 0 | **Build Tools + .NET Framework SDK installed.** Rob ran the Visual Studio 2022 Build Tools installer (17.14.37614: `VCTools` workload, MSVC 14.44.35207, Windows SDK 10.0.26100, .NET Framework 4.6.2 targeting pack) after the two winget attempts had been cancelled at UAC. The first `build_editor.ps1` failed at rules time (43.62 s): `SwarmInterface.Build.cs` throws `Could not find NetFxSDK install dir ... Install a version of .NET Framework SDK at 4.6.0 or higher`; `setup.exe modify` for `Microsoft.Net.Component.4.8.SDK` returned exit 5007 (reboot pending), so the standalone **.NET Framework 4.8.1 Developer Pack** was installed via winget (`Microsoft.DotNet.Framework.DeveloperPack_4`), creating `C:\Program Files (x86)\Windows Kits\NETFXSDK\4.8.1`; UBT accepted it. Section 1 rows updated; `Docs/TOOLCHAIN-SETUP.md` step 2b added and its v14.50 `TODO(VERIFY 5.8)` resolved (UBT accepts MSVC 14.44, no toolset complaint). |
| 2026-09-06 | 0 | **First compile.** `generate_project_files.ps1` OK (18.5 s, `Hellfall.slnx`; `*.slnx` and `.vsconfig` git-ignored in `7b3e60f`). `build_editor.ps1`: the first real compile (99.73 s) produced exactly one error pair, `HellfallTuning.cpp(324)` C2228 / C2665 - `const FKey Key(FName(*KeyName));` parsed as a function declaration - fixed to `const FKey Key = FKey(FName(*KeyName));` in `49e9696`; rebuild (8.17 s) `Result: Succeeded`, `UnrealEditor-Hellfall.dll` 488,448 bytes, no other errors or warnings from the module. |
| 2026-09-06 | 0 | **Maps regenerated with the final generators** (even fill lights, single-sided labels, non-colliding figures, level-recreate fix - `delete_asset` returns True in the commandlet without deleting the `.umap`, so `_recreate_level` loads and clears the existing map; create and reuse paths both verified): `L_ExecutiveFloor` 249 actors / 450,385 bytes, `L_FeelGym` 171 actors / 313,898 bytes, 24 `M_GB_*` materials; **committed in `12faf06`** (LFS 26 objects / 894 KB). |
| 2026-09-06 | 0 | **gate-0 package built and smoke-tested** (1.3): `package.ps1 -Tag gate-0` BUILD SUCCESSFUL in 211 s (UAT `0h 3m 3s`); `Builds\Hellfall-gate-0-Win64-Development.zip` **327.1 MB (342,939,354 bytes), SHA256 `76E2EC623A5B58D7CDB04D96AAA3813E081D38522D46D0FF7FA054E9156149A2`**; the packaged `Hellfall.exe` booted both maps on the iGPU under DX12 SM6 with `HellfallGameMode`, tuning loaded from the staged `Data\`, 12 key mappings, HUD and labels rendered, one expected warning (72 cm crawl clamp). `main` = `49e9696` (`2c27652` -> `12faf06` -> `7b3e60f` -> `49e9696`), pushed; the `gate-0` Release is published with `GATE-0-NOTES.md` as its body. Gate 1 review starts when Rob walks the build; nothing in Gate 1 is claimed as met. |
| 2026-09-06 | 1 | **Rob's gate-0 review** (from his phone, on the release screenshots): add ceiling and walls to the feel gym; a whole legal-office floor, not too big; a tiny amount of texture on floor, ceiling and walls; a bit of a maze with slots to stage monsters and scenes; about ten minutes to explore. Recorded as gate-0 feedback driving gate-1 (decision 3.23: Gate 1 not yet approved, so no approved scope reopened; the spec adjacency stays the spine). |
| 2026-09-06 | 1 | **Tooling for the gate-1 brief, commit `e92bea2`** (tools owner): world-aligned surface texture per surface class (engine grid texture through the engine `WorldAlignedTexture` function, lerped into each tint at floor 0.14 / ceiling 0.10 / wall 0.06 / duct 0.10, tiles 50 / 60 / 100 / 25 cm; tints darkened - floors ~0.5, ceilings 0.72, walls ~0.85; decision 3.26); feel gym rebuilt as an enclosed hall (walls, 450 ceiling, 48 grid fill lights, no sun or sky; 3.27); `scenes` staging slots with kind-tinted markers, `KIND: id` labels and wrapped descriptions (3.29); note labels 16 -> 11 cm and a `labels.notes_enabled` switch; validator exploration estimate + critical-path length + junction count; `check_manifest.mjs` enclosure check (3.33). Editor run: all 11 architectural materials built textured, no fallback. Materials under `Content/Greybox/Materials` now number 29 (5 new `scene_*` tints). |
| 2026-09-06 | 1 | **Law-firm floor designed and promoted** (lead, after three designer agents failed on output size): `Docs/candidates/legal_lead.build.mjs` -> `Data/candidates/legal_lead.json` -> `Data/floorplan.json`; the gate-0 plan kept as `Data/candidates/floorplan_gate0.json` (3.24). 37 rooms / 38 openings / 11 scenes, 5500 x 4800 cm incl. walls, ring around a service core + 2 south spurs + collapsed spur + bullpen shortcut + elevator lobby on the exterior east wall; critical path ~103 m over 8 legs through the west lobby (Demon #1), exploration ~10.8 min by the new model (3.30; the earlier walking-only x1.6 model gave ~4 min and was replaced). **Validator rule changes** (3.25, 3.33): transit rooms <= 320 wide count as corridors, wider are lobbies (INFO); Demon #1 in the room Office #2's door opens into; model constants from `Data/metrics.json`. Generator: label facing rule (c') for rooms off non-critical spurs faces any neighbouring door. `Docs/FLOORPLAN.md` rewritten for the floor; placement fixes from its audit applied (3.34). Dry run: 590 actors, 316 collision boxes, 0 overlaps, enclosure 37/37, money shot 86.3 deg = 96 % (71 % net of the divider). |
| 2026-09-06 | 1 | **Auto exposure re-enabled + lights toned down** (3.35): the second gate-1 sweep was still white where 2-3 grid lights overlap -> `r.DefaultFeature.AutoExposure=True` (histogram, bias 0), intensities x0.65, radius 500-800; gate-1 package rebuilt. |
| 2026-09-06 | 1 | **Light grid fix** (3.28): the first gate-1 screenshot sweep was blown out to white - one light per room with radius 1.25 x the room diagonal gave the 44.8 m south corridor a 56 m light. Rooms now get a light grid at most `light_spacing_cm` (800) apart with the radius clamped to `max_attenuation_radius_cm` (1000): 59 fill lights on the floor, 48 + 12 in the gym. Maps regenerated: `L_ExecutiveFloor` 590 actors, `L_FeelGym` 222 actors. |
| 2026-09-06 | 1 | **gate-1 package** (1.4): `package.ps1 -Tag gate-1` -> `Builds\Hellfall-gate-1-Win64-Development.zip`, 327.6 MB (343,510,411 bytes), SHA256 `9B24B3B323B72F52C09C52A4C4C77A7AB608890E47F19F47744078A3D32D92B2`, 122 s; git revision `e92bea2` in `BUILD-INFO.txt`. Review tooling shipped alongside (3.32): `?spawn=X,Y,Z,Yaw` in `AHellfallGameMode`, `Tools/screenshot_sweep.ps1` + `Data/review_shots.json` (31 viewpoints), `Tools/contact_sheet.ps1`; the root `Hellfall.exe` is a bootstrapper for `Hellfall\Binaries\Win64\Hellfall.exe`. Published as the `gate-1` Release with `GATE-1-NOTES.md` as its body. Not verified by the agent (Rob's Gate 1 walk): feel, duct gate, collision, stand-blocked, F1, focus loss, 60 fps at 1080p, the 10 minutes in practice, whether the maze reads. |
| 2026-09-07 | 1 | **Gate 1 APPROVED by Rob** on the gate-1 release. `Docs/METRICS.md` frozen (FROZEN header), `Data/metrics.json` status FROZEN, greybox scope frozen under change control. Gate 2 (blockout props and composition) begins. |
| 2026-09-07 | 2 | **REQ-G2-001 asset audit done in Blender 5.2.1** (headless `Tools/blender/audit_glb.py`, `--background --factory-startup`, bundled glTF importer 5.2.40): 17 GLBs, 0 errors, 6.3 s -> `Docs/audit/blender_audit_2026-09-07.md` + `.json`. Findings: the demons are 1.70 m tall (the node parser had mis-scaled skinned meshes 100x; scale 1.29x / 1.76x to 220 / 300, not 130x / 176x); the guard is a seated slump 78 x 161 x 82, not prone; the toilet is wall-hung; the reception desk is a 157 x 157 diorama at desk height; the couch set an L 280 x 254 x 80; door 102 x 17 x 220 and elevator 134 x 21 x 220 (height wins over the frozen openings, greybox fills the flanks); every budget fits except the couch 8,104 / 8,000 (1.3 % over, inside tolerance). Section 5 rewritten with the audited targets (one driving dimension, uniform scale; 3.36). Only rejection: `crimson_hellfiend_texture_0_1.png`, a byte-identical duplicate - `git rm` from `SourceAssets/` (125 files); the raw bundle's ` - Copy` / `deamon` / `Meshy_AI_` names were resolved by the earlier Godot pass and are not on this machine (documented, section 5 "Provenance"). |
| 2026-09-07 | 2 | **Blockout data + validator** (REQ-G2-002..005): `Docs/blockout/blockout.build.mjs` -> `Data/blockout.json` - 192 props (17 audited slots -> 21 blockouts + 175 generic law-firm kit pieces, 3.38), 4 characters, 1 pickup with a 160 x 62 x 60 halo, 2 demon cylinders, 2 trigger volumes, the dwell volume, the backdrop plane - all INSIDE the approved rooms, `Data/floorplan.json` untouched. Schema in `Docs/FLOORPLAN-SCHEMA.md` "Blockout data"; rationale in `Docs/blockout/BLOCKOUT.md`; `Docs/blockout/blockout.tables.mjs` regenerates its tables and the manifest column. Generator (`Tools/ue/build_greybox.py`) auto-loads the file for `Data/floorplan.json`; without it the build is the approved greybox byte for byte. New `Tools/validate_blockout.mjs` (>= 5 cm to walls or flush, no overlaps, 76 door-swing zones, lanes widened by the 36 cm capsule, checkpoint / start discs, scene rects with the tableau rule 3.39, the +-90 cm money-shot strip, corridor clear width >= 216 past props, start -> duct sweep, demon routes and chokepoint >= 280): PASS. `check_manifest.mjs` blockout + label checks; `test_dry_run.ps1` 6a-6d. Dry run: 999 actors, 513 collision boxes (4 yawed), 0 overlaps, 197 blockouts inside their rooms, enclosure 37/37, money shot 86.3 deg = 96 %; `test_dry_run.ps1` 14/14 PASS. `Data/greybox_style.json` gained the `blockout` block and 13 tints (42 `M_GB_*` materials). |
| 2026-09-07 | 2 | **Guard decision + verifier fixes**: the guard back on `corridor_main`'s north wall (x 4300-4461, 202 clear) under the CHARACTER-only 150 cm body waiver (3.37), replacing the first cut's break-room placement; each demon cylinder on its audited footprint slab (162 x 204 / 208 x 83 x 10, no collision); trigger / dwell volumes toned to opacity 0.1 so they do not wash the money shot, Demon #2's label lifted under the ceiling clamp, the boundary shell hidden in the editor viewport as well as in game (3.40); the backdrop enlarged to 12000 x 5000 at z -2500 and checked from the dwell corners (3.43); closed-door leaves modelled as `SM_ClosedDoor` boxes IN the two LOCKED openings, 17 cm proud (3.41); the seated man moved to a north rack section so the start -> duct route has 63 cm clearance (3.44); CEO desk WEST of the axis per the approved note (3.42); tableau props back in their scene rects (3.39); corridors dressed (3.38). `Data/review_shots.json` -> 41 viewpoints; `screenshot_sweep.ps1 -Tag gate-2` + contact sheets. Open for Rob: guard (elevator alcove?), CEO desk east vs west, reception desk on-axis vs slid east, backdrop plane vs sphere, door leaves proud of the infills, closed leaves only on the LOCKED doors (`GATE-2-NOTES.md` section 5). |
| 2026-09-07 | 2 | **gate-2 package** (1.5): `package.ps1 -Tag gate-2` -> `Builds\Hellfall-gate-2-Win64-Development.zip`, 327.9 MB (343,807,217 bytes), SHA256 `DCCF4C8999CA622FED6635F340772EB5B8D220743CDBAD78D7046719E940C8D3`, 164 s. `L_ExecutiveFloor` regenerated with the `Blockout/*` folders (`L_ExecutiveFloor.umap` 1,744,821 bytes), `L_FeelGym` unchanged, 42 materials. Published as the `gate-2` Release with `GATE-2-NOTES.md` as its body: https://github.com/RobleusCaesar/hellfall/releases/tag/gate-2. Not verified by the agent (Rob's Gate 2 walk): walking every route around the blockouts without catching, whether rooms read occupied / crowded / empty, the pushed-out chair, the guard and shotgun being where you naturally look, backing away from Demon #1, the money shot with props in view, the two survivors in the closet. |
